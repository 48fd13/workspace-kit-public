# Runtime Bootstrap

Read and follow the nearest project `AGENTS.md`.

Do not create project workflow-control files unless the user asks to initialize or change the workspace system.

Respect configured runtime permissions. Ask before destructive, irreversible, machine-wide, external, deploy/publish, dependency, infrastructure, secrets/auth, billing/payment, or data-loss operations.

Do not hardcode secrets. Do not commit unless explicitly asked.
