# Claude Code Instructions

Read `AGENTS.md` first.

If `_workspace/map/routing-table.md` exists, use it to select the active workflow and read only the required workflow, runbook, and context files.

`AGENTS.md` is the canonical instruction file when instructions overlap.

For creating, rebuilding, renaming, or repairing the workspace kit itself, use `docs/workspace-kit-bootstrap.md`.
