# Helm / Kustomize Notes

Review rendered manifests, values overlays, environment differences, secrets handling, chart dependencies, hooks, CRDs, upgrade/rollback behavior, and drift between declared and live state.
