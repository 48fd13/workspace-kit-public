# Stage 00: Intake

## Inputs

| Layer | Path | Use |
|---|---|---|
| Layer 4 working | `_workspace/runs/active/<run-slug>/input/` | Current onboarding source material |
| Layer 3 reference | `../../_config/` | Stable preferences if present |

## Do NOT load

- later stage folders

## Process

1. Clarify scope, systems, environments, known constraints, and desired map outputs.
2. Identify missing source material and obvious open questions.

## Outputs

Write `00_intake.md` to `_workspace/runs/active/<run-slug>/stages/`.

## Review gate

Stop for review before inventory.
