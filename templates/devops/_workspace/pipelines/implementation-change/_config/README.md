# Config

Store pipeline-specific conventions here when this pipeline is customized for a team or repo.

Examples:

- validation command policy
- branch/review expectations
- release constraints
- required artifact names

Do not store secrets or environment credentials.
