# Shared Reference

Place stable reference material shared by all stages here.

Examples:

- service/module ownership notes
- architecture sketches
- validation matrix
- coding or config conventions

Keep volatile run-specific material under `_workspace/runs/active/<run-slug>/` instead.
