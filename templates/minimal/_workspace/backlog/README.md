# Backlog

Deferred work, follow-ups, risks to revisit, and open action items.

Use one file per item under `items/`. Keep completed artifacts such as plans, reviews, validations, and handoffs in `_workspace/outputs/`.
