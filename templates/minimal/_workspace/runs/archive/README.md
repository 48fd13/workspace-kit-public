# Archived Runs

Explicitly archived finished-run history.

Archive only on explicit user instruction after the run is `finished` and its durable output paths are recorded. Set status to `archived` before moving the run here.
