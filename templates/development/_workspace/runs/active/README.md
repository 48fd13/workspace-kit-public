# Active Runs

Current in-progress runs.

Folder name:

```text
YYYY-MM-DD-topic-slug/
```

Workflow run shape:

```text
YYYY-MM-DD-topic-slug/
├── RUN.md
├── input/
├── output/
└── final/
```

Pipeline run shape:

```text
YYYY-MM-DD-topic-slug/
├── RUN.md
├── input/
├── stages/
└── final/
```

Ask before moving or clearing active run folders.
