# Dependency Upgrade Review

Use `_workspace/runbooks/dependency-upgrade-safety.md`.

Check release notes, breaking changes, transitive dependencies, install scripts, advisories, licenses, runtime compatibility, tests, and rollback.

Do not install dependencies unless explicitly approved.
