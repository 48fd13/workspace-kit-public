# Configuration Change Review

Check environment-specific behavior, secrets vs config, defaults, rollout/rollback, config drift, validation, and docs/runbook updates.

Flag any config that can change production behavior.
