# Containerization Review

Use `_workspace/runbooks/docker-container-safety.md`.

Check base image trust, pinned versions, build context, secrets in layers, non-root user, ports/envs, healthchecks, image size, and reproducibility.
