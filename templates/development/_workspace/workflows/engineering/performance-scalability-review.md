# Performance / Scalability Review

Check N+1 queries, unbounded loops, pagination, cache correctness, slow DB queries, queue throughput, retries/timeouts, connection pools, memory/CPU limits, and large allocations.

Avoid micro-optimizations without evidence.
