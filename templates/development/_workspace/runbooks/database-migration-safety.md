# Database Migration Safety

Check compatibility with old/new app versions, lock duration, table size, online index strategy, nullable/default/backfill order, retry/idempotency, rollback/forward-fix, validation queries, and backup/snapshot needs.
