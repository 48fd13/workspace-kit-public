# Observability Instrumentation

Prefer structured logs with correlation IDs, metrics for rate/errors/latency/saturation/queue depth, traces around external calls, actionable error context without secrets/PII, and low-cardinality labels.
