# Git Commit Runbook

Do not commit unless explicitly asked.

When asked: inspect status, diff, and recent commit style; stage only relevant files; avoid secrets; write a concise why-focused message; check status after commit.
