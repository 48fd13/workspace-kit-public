# Docker / Container Safety

Check trusted/pinned base images, minimal final image, `.dockerignore`, secrets in layers, non-root user, ports/envs, healthcheck, deterministic install, and unnecessary tools in final image.
