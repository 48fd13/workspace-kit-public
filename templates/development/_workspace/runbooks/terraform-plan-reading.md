# Terraform Plan Reading

Summarize create/update/delete/replace counts. Call out replacements and destroys first. Inspect IAM, networking, security groups, storage, DNS, databases, provider/module versions, state/backend assumptions, variables, and cost-impacting resources.

Require explicit approval before apply.
